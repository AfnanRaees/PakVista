name: CI Dev

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dev pipeline placeholder
        run: |
          echo "Running CI Dev workflow"
